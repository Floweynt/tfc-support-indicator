# TFC Support Indicator
Adds a tooltip in jade that tells you if a rock is supported or not.

You can download from modrinth, github releases or build it yourself (`gradlew build`). 

Backports will be considered on request. 
